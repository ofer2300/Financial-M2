import React from 'react';

export const MeetingTypeSelector = ({ onChange }: { onChange: (value: string) => void }) => {
  // לוגיקה לבחירת סוג פגישה
  return (
    <div>
      {/* ממשק לבחירת סוג פגישה */}
    </div>
  );
}; 