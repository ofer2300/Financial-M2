import React from 'react';

export const RecurrenceSelector = ({ onChange }: { onChange: (value: string) => void }) => {
  // לוגיקה לבחירת חזרתיות
  return (
    <div>
      {/* ממשק לבחירת חזרתיות */}
    </div>
  );
}; 