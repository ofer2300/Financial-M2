import React from 'react';

export const ParticipantSelector = ({ onSelect }: { onSelect: (participants: any[]) => void }) => {
  // לוגיקה לבחירת משתתפים
  return (
    <div>
      {/* ממשק לבחירת משתתפים */}
    </div>
  );
}; 